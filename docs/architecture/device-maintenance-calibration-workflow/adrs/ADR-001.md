```json
[
  {
    "number": 1,
    "title": "Multi-Tenant Device Maintenance Domain Model with Aggregate Boundaries",
    "rendered_markdown": "# ADR-001: Multi-Tenant Device Maintenance Domain Model with Aggregate Boundaries\n\n## Status\n\n| **Status** | Proposed |\n|------------|----------|\n| **Date** | 2024-12-19 |\n| **Authors** | Enterprise Solution Architecture |\n| **Reviewers** | Security Architecture, Compliance Team |\n| **Stakeholders** | Healthcare Vendors, Platform Engineering |\n\n## Context\n\nThe Device Maintenance & Calibration Workflow requires a domain model that supports:\n- Multi-tenant isolation with strict data boundaries per healthcare vendor\n- Complex maintenance workflows spanning sanitization, calibration, and telemetry testing\n- Regulatory compliance (FDA 21 CFR Part 820, ISO 13485) with complete audit trails\n- Integration with existing Curetor Marketplace authentication and inventory systems\n- Device lifecycle management from \"Needs Maintenance\" through \"Ready for Deployment\"\n\nThe existing platform provides foundational patterns in `src/Domain/Common/BaseEntity.cs`, `src/Infrastructure/Data/TenantDbContext.cs`, and audit logging infrastructure, but lacks device-specific domain modeling.\n\n## Decisions\n\n### D-01: Implement Device Aggregate as Primary Domain Boundary\n\n**Decision:** Create a `Device` aggregate root that encapsulates device identity, status, and maintenance scheduling, with `MaintenanceWorkflow` as a separate aggregate that references Device by ID.\n\n**Rationale:** \n- Device has clear business identity (SerialNumber) and lifecycle independent of maintenance activities\n- Maintenance workflows can span multiple sessions and require separate transactional boundaries\n- Supports concurrent maintenance operations on different devices without blocking\n- Aligns with DDD aggregate design principles for consistency boundaries\n\n### D-02: Separate Maintenance Workflow Aggregate with Event-Driven Coordination\n\n**Decision:** Model `MaintenanceWorkflow` as an independent aggregate containing `SanitizationChecklist`, `CalibrationTest[]`, and `TelemetryTest[]` as entities within the aggregate boundary.\n\n**Rationale:**\n- Maintenance workflow has distinct lifecycle from device (started, in-progress, completed)\n- Enables atomic updates to all maintenance activities within a single transaction\n- Supports workflow state machine transitions with strong consistency\n- Allows multiple technicians to work on different workflow aspects concurrently\n\n### D-03: Multi-Tenant Isolation via Global Query Filters and Tenant-Scoped Entities\n\n**Decision:** Implement tenant isolation using EF Core global query filters on all entities, with `TenantId` as a required foreign key on every table.\n\n**Rationale:**\n- Leverages existing `TenantDbContext.cs` infrastructure with proven security model\n- Prevents accidental cross-tenant data access at the database level\n- Supports tenant-specific maintenance procedures and compliance requirements\n- Enables efficient tenant-scoped queries with proper indexing strategy\n\n### D-04: Encrypted Sensitive Fields with Audit Trail Integration\n\n**Decision:** Apply field-level encryption to technician PII (`FirstName`, `LastName`, `Email`) and file paths using existing `EncryptedFieldAttribute` infrastructure.\n\n**Rationale:**\n- Maintains HIPAA compliance for workforce member information\n- Protects calibration certificate file locations from unauthorized access\n- Integrates with existing encryption key management in `src/Infrastructure/Security/`\n- Supports audit requirements without exposing sensitive data in logs\n\n## Alternatives Considered\n\n### Alternative A-01: Single Large Maintenance Aggregate\n**Rejected:** Combining Device and MaintenanceWorkflow into one aggregate would create large transaction boundaries, increase lock contention, and violate single responsibility principle.\n\n### Alternative A-02: Event Sourcing for Maintenance History\n**Rejected:** While providing complete audit trails, event sourcing adds complexity that exceeds current platform capabilities and regulatory requirements can be met with traditional audit logging.\n\n### Alternative A-03: Document-Based Storage for Workflow Data\n**Rejected:** JSON/document storage would complicate multi-tenant queries, indexing, and referential integrity enforcement required for regulatory compliance.\n\n### Alternative A-04: Shared Database with Application-Level Tenant Filtering\n**Rejected:** Application-level filtering increases risk of data leakage and doesn't provide defense-in-depth security required for healthcare data.\n\n## Consequences\n\n### Positive Consequences\n- **Regulatory Compliance:** Complete audit trails with encrypted PII meet HIPAA and FDA requirements\n- **Scalability:** Separate aggregates enable concurrent maintenance operations without blocking\n- **Security:** Multi-layered tenant isolation with database-level query filters\n- **Maintainability:** Clear domain boundaries align with business processes and team responsibilities\n- **Integration:** Leverages existing platform authentication, authorization, and audit infrastructure\n\n### Negative Consequences / Trade-offs\n- **Complexity:** Multiple aggregates require careful coordination via domain events\n- **Performance:** Encrypted fields add computational overhead for sensitive data operations\n- **Storage:** Audit trails and soft delete patterns increase database storage requirements\n- **Development Effort:** Custom domain model requires more implementation time than generic CRUD\n\n### Risks and Mitigations\n\n| **Risk** | **Impact** | **Mitigation** |\n|----------|------------|----------------|\n| Cross-tenant data leakage | High | Global query filters + integration tests + security reviews |\n| Aggregate coordination failures | Medium | Eventual consistency patterns + compensating transactions |\n| Encryption key management issues | High | Leverage existing key rotation + backup procedures |\n| Performance degradation under load | Medium | Database indexing strategy + connection pooling + monitoring |\n\n## Compliance & Security Impact\n\n- **Clean Architecture:** Domain model in `src/Domain/Maintenance/` with clear separation from infrastructure concerns, following existing patterns in `src/Domain/Common/`\n- **Multi-Tenancy & Data Isolation:** Global query filters ensure tenant isolation at database level, with `TenantId` foreign keys on all entities\n- **HIPAA / Auditability:** Encrypted PII fields, comprehensive audit trails via `AuditableEntity` base class, and soft delete for data retention compliance\n- **GDPR:** Right to erasure supported via soft delete patterns, encrypted fields support data minimization\n- **PCI-DSS:** No payment card data in maintenance workflows, but encryption patterns support future payment integration\n\n## Validation\n\n- [ ] Domain model review with healthcare compliance team\n- [ ] Multi-tenant isolation testing with penetration testing team\n- [ ] Performance testing with 1000+ devices per tenant\n- [ ] Integration testing with existing authentication and audit systems\n- [ ] Security review of encryption implementation and key management"
  },
  {
    "number": 2,
    "title": "PostgreSQL Schema Design with Regulatory Compliance Constraints",
    "rendered_markdown": "# ADR-002: PostgreSQL Schema Design with Regulatory Compliance Constraints\n\n## Status\n\n| **Status** | Proposed |\n|------------|----------|\n| **Date** | 2024-12-19 |\n| **Authors** | Enterprise Solution Architecture |\n| **Reviewers** | Database Architecture, Compliance Team |\n| **Stakeholders** | Healthcare Vendors, Platform Engineering |\n\n## Context\n\nThe Device Maintenance & Calibration Workflow requires a PostgreSQL schema that supports:\n- FDA 21 CFR Part 820 quality management requirements with complete traceability\n- ISO 13485 medical device quality management with calibration records\n- HIPAA compliance for workforce member information and audit trails\n- Multi-tenant data isolation with performance at scale (1000+ devices per tenant)\n- Integration with existing EF Core Code-First patterns in `src/Infrastructure/Data/`\n\nThe existing platform uses PostgreSQL 16 with EF Core migrations, `xmin` concurrency tokens, and established audit logging patterns in `TenantDbContext.cs`.\n\n## Decisions\n\n### D-01: Comprehensive CHECK Constraints for Data Integrity\n\n**Decision:** Implement database-level CHECK constraints for all business rules including value ranges, string lengths, and cross-column validations.\n\n**Rationale:**\n- FDA 21 CFR Part 820.70(i) requires validation of data integrity at all levels\n- Database constraints provide defense-in-depth beyond application validation\n- Prevents data corruption from direct database access or integration failures\n- Supports regulatory audit requirements for data quality controls\n\n**Examples:**\n```sql\n-- Calibration results must have positive tolerance ranges\nCHECK (ToleranceRange >= 0)\n\n-- Battery capacity must be realistic percentage\nCHECK (CapacityPercentage BETWEEN 0 AND 100)\n\n-- File sizes must be within upload limits\nCHECK (FileSize > 0 AND FileSize <= 52428800) -- 50MB limit\n```\n\n### D-02: JSONB for Flexible Test Parameters with Structured Validation\n\n**Decision:** Use PostgreSQL JSONB columns for `CalibrationTest.TestParameters` and `TelemetryTest.TestResults` with application-level schema validation.\n\n**Rationale:**\n- Different device types require different calibration parameters (O2 sensors vs ECG vs flow meters)\n- JSONB provides indexing capabilities for query performance\n- Maintains schema flexibility while preserving audit trail completeness\n- Supports future device types without schema migrations\n\n**Structure Examples:**\n```json\n// O2 Sensor Calibration Parameters\n{\n  \"targetConcentration\": 21.0,\n  \"tolerancePercent\": 2.0,\n  \"testDurationSeconds\": 300,\n  \"ambientTemperature\": 22.5,\n  \"ambientHumidity\": 45.0\n}\n\n// Telemetry Test Results\n{\n  \"cellularSignalStrength\": -65,\n  \"wifiConnected\": true,\n  \"dataTransmissionRate\": 1.2,\n  \"protocolVersion\": \"HL7-FHIR-R4\",\n  \"encryptionEnabled\": true\n}\n```\n\n### D-03: Computed Columns for Regulatory Calculations\n\n**Decision:** Implement PostgreSQL GENERATED ALWAYS AS columns for calculated fields like deviation percentages and compliance indicators.\n\n**Rationale:**\n- Ensures calculation consistency across all applications and reports\n- Provides immutable audit trail of calculated values\n- Supports regulatory requirements for traceability of derived data\n- Eliminates calculation errors from application code variations\n\n**Implementation:**\n```sql\n-- Automatic calculation of calibration deviation\nDeviationPercentage decimal GENERATED ALWAYS AS \n  (ABS((MeasuredValue - TargetValue) / TargetValue * 100)) STORED\n```\n\n### D-04: Encrypted Field Strategy for PII and Sensitive Paths\n\n**Decision:** Apply field-level encryption to technician PII and file paths using existing `EncryptedFieldAttribute` infrastructure with explicit column annotations.\n\n**Rationale:**\n- HIPAA requires protection of workforce member information\n- File paths may contain sensitive device or customer identifiers\n- Leverages existing encryption key management and rotation procedures\n- Maintains queryability for non-encrypted fields while protecting sensitive data\n\n### D-05: Comprehensive Indexing Strategy for Multi-Tenant Performance\n\n**Decision:** Implement composite indexes with `TenantId` as the leading column for all tenant-scoped queries.\n\n**Rationale:**\n- Ensures efficient tenant isolation without full table scans\n- Supports common query patterns (device lookup, maintenance history, status filtering)\n- Prevents performance degradation as tenant data grows\n- Aligns with existing indexing patterns in `TenantDbContext.cs`\n\n**Key Indexes:**\n```sql\n-- Device lookups by tenant and status\nCREATE INDEX IX_Device_TenantId_DeviceStatus_NextCalibrationDue \n  ON Device (TenantId, DeviceStatus, NextCalibrationDue);\n\n-- Maintenance workflow queries\nCREATE INDEX IX_MaintenanceWorkflow_TenantId_DeviceId_Status \n  ON MaintenanceWorkflow (TenantId, DeviceId, WorkflowStatus);\n\n-- Audit trail queries\nCREATE INDEX IX_DeviceStatusHistory_TenantId_DeviceId_StatusChangeDate \n  ON DeviceStatusHistory (TenantId, DeviceId, StatusChangeDate DESC);\n```\n\n## Alternatives Considered\n\n### Alternative A-01: NoSQL Document Storage for Maintenance Data\n**Rejected:** While flexible, document storage complicates multi-tenant queries, referential integrity, and regulatory audit requirements that demand structured relational data.\n\n### Alternative A-02: Separate Databases Per Tenant\n**Rejected:** Increases operational complexity, backup/restore procedures, and doesn't align with existing platform multi-tenancy patterns.\n\n### Alternative A-03: Application-Level Encryption Only\n**Rejected:** Database-level encryption provides additional security layer and supports compliance requirements for data at rest protection.\n\n### Alternative A-04: Generic EAV Model for Test Parameters\n**Rejected:** Entity-Attribute-Value patterns sacrifice type safety, query performance, and complicate audit trail analysis required for regulatory compliance.\n\n## Consequences\n\n### Positive Consequences\n- **Regulatory Compliance:** Database constraints and audit trails meet FDA and ISO requirements\n- **Data Integrity:** CHECK constraints prevent invalid data at the database level\n- **Performance:** Proper indexing supports efficient multi-tenant queries at scale\n- **Flexibility:** JSONB columns accommodate diverse device types and test parameters\n- **Security:** Field-level encryption protects PII while maintaining query performance\n- **Auditability:** Complete schema supports regulatory audit and traceability requirements\n\n### Negative Consequences / Trade-offs\n- **Storage Overhead:** Audit trails, indexes, and encryption increase storage requirements by ~30%\n- **Migration Complexity:** CHECK constraints require careful data validation during deployment\n- **Query Complexity:** JSONB queries require specialized knowledge and careful indexing\n- **Backup Size:** Encrypted fields and audit data increase backup storage and time\n\n### Risks and Mitigations\n\n| **Risk** | **Impact** | **Mitigation** |\n|----------|------------|----------------|\n| CHECK constraint violations during migration | High | Comprehensive data validation scripts + rollback procedures |\n| JSONB query performance degradation | Medium | GIN indexes on JSONB columns + query optimization |\n| Encryption key rotation downtime | Medium | Blue-green deployment + key rotation procedures |\n| Index maintenance overhead | Low | Automated index maintenance + monitoring alerts |\n\n## Compliance & Security Impact\n\n- **Clean Architecture:** Database schema in `src/Infrastructure/Data/Configurations/` follows EF Core Code-First patterns with clear separation from domain logic\n- **Multi-Tenancy & Data Isolation:** `TenantId` foreign keys with global query filters ensure complete tenant isolation at database level\n- **HIPAA / Auditability:** \n  - Encrypted PII fields for workforce members\n  - Complete audit trails with immutable timestamps\n  - Soft delete patterns for data retention requirements\n  - CHECK constraints ensure data quality for regulatory compliance\n- **GDPR:** \n  - Right to erasure via soft delete patterns\n  - Data minimization through encrypted field strategy\n  - Audit trail supports data processing transparency\n- **PCI-DSS:** Schema design supports future payment integration with appropriate encryption patterns\n\n## Validation\n\n- [ ] Database schema review with DBA team and compliance officers\n- [ ] Performance testing with 10,000+ devices across 100+ tenants\n- [ ] Encryption implementation validation with security team\n- [ ] CHECK constraint testing with invalid data scenarios\n- [ ] Index performance analysis with realistic query patterns\n- [ ] Backup/restore testing with encrypted data\n- [ ] Regulatory audit simulation with sample data"
  },
  {
    "number": 3,
    "title": "Device Status State Machine with Regulatory Compliance Tracking",
    "rendered_markdown": "# ADR-003: Device Status State Machine with Regulatory Compliance Tracking\n\n## Status\n\n| **Status** | Proposed |\n|------------|----------|\n| **Date** | 2024-12-19 |\n| **Authors** | Enterprise Solution Architecture |\n| **Reviewers** | Healthcare Compliance, Quality Assurance |\n| **Stakeholders** | Healthcare Vendors, Regulatory Affairs |\n\n## Context\n\nThe Device Maintenance & Calibration Workflow requires a robust state machine to manage device lifecycle transitions while maintaining regulatory compliance:\n- FDA 21 CFR Part 820 requires complete traceability of device status changes\n- ISO 13485 mandates documented procedures for device maintenance and calibration\n- Healthcare vendors need clear visibility into device readiness for patient use\n- Integration with existing inventory systems requires reliable status synchronization\n- Multi-tenant isolation must be maintained throughout status transitions\n\nThe existing platform provides domain event infrastructure in `src/Domain/Common/DomainEvent.cs` and audit logging via `AuditableEntity.cs`, but lacks device-specific state management.\n\n## Decisions\n\n### D-01: Comprehensive Device Status Enumeration with Regulatory Alignment\n\n**Decision:** Implement a `DeviceStatus` enumeration that covers the complete device lifecycle from acquisition through disposal, with explicit regulatory compliance states.\n\n**Rationale:**\n- FDA 21 CFR Part 820.70 requires identification and traceability throughout device lifecycle\n- Clear status definitions prevent ambiguity in device readiness for patient use\n- Supports automated compliance reporting and audit trail generation\n- Enables efficient inventory management and deployment planning\n\n**Status Definitions:**\n```csharp\npublic enum DeviceStatus\n{\n    // Initial States\n    Available = 1,           // Ready for rental deployment\n    Deployed = 2,            // Currently in patient use\n    \n    // Maintenance States\n    NeedsMaintenance = 10,   // Returned from rental, requires service\n    InMaintenance = 11,      // Currently undergoing maintenance workflow\n    CalibrationDue = 12,     // Scheduled calibration required\n    InCalibration = 13,      // Currently undergoing calibration\n    \n    // Problem States\n    RequiresRepair = 20,     // Failed maintenance, needs deep repair\n    InRepair = 21,           // Currently undergoing repair\n    \n    // Final States\n    ReadyForDeployment = 30, // Maintenance/calibration complete\n    Quarantined = 40,        // Isolated due to safety/compliance issues\n    Decommissioned = 50      // End of lifecycle, removed from service\n}\n```\n\n### D-02: State Machine with Validation Rules and Audit Integration\n\n**Decision:** Implement state transition validation using a state machine pattern with domain events for audit trail and integration notifications.\n\n**Rationale:**\n- Prevents invalid state transitions that could compromise patient safety\n- Generates automatic audit trails for regulatory compliance\n- Enables real-time inventory synchronization via domain events\n- Supports business rule enforcement (e.g., calibration requirements)\n\n**Valid Transitions:**\n```csharp\npublic static readonly Dictionary<DeviceStatus, DeviceStatus[]> ValidTransitions = new()\n{\n    [DeviceStatus.Available] = new[] { DeviceStatus.Deployed, DeviceStatus.CalibrationDue, DeviceStatus.InMaintenance },\n    [DeviceStatus.Deployed] = new[] { DeviceStatus.NeedsMaintenance, DeviceStatus.Available },\n    [DeviceStatus.NeedsMaintenance] = new[] { DeviceStatus.InMaintenance, DeviceStatus.Quarantined },\n    [DeviceStatus.InMaintenance] = new[] { DeviceStatus.ReadyForDeployment, DeviceStatus.RequiresRepair },\n    [DeviceStatus.CalibrationDue] = new[] { DeviceStatus.InCalibration, DeviceStatus.Quarantined },\n    [DeviceStatus.InCalibration] = new[] { DeviceStatus.ReadyForDeployment, DeviceStatus.RequiresRepair },\n    [DeviceStatus.RequiresRepair] = new[] { DeviceStatus.InRepair, DeviceStatus.Decommissioned },\n    [DeviceStatus.InRepair] = new[] { DeviceStatus.ReadyForDeployment, DeviceStatus.Decommissioned },\n    [DeviceStatus.ReadyForDeployment] = new[] { DeviceStatus.Available },\n    [DeviceStatus.Quarantined] = new[] { DeviceStatus.InMaintenance, DeviceStatus.Decommissioned },\n    [DeviceStatus.Decommissioned] = new DeviceStatus[0] // Terminal state\n};\n```\n\n### D-03: Automatic Calibration Due Date Calculation with Device-Specific Intervals\n\n**Decision:** Implement automatic calculation of next calibration due dates based on device type, manufacturer requirements, and regulatory standards.\n\n**Rationale:**\n- ISO 13485 requires documented calibration intervals for measurement equipment\n- Different device types have different calibration requirements (O2 sensors: 6 months, ECG: 12 months)\n- Automated scheduling prevents compliance violations and ensures device readiness\n- Supports proactive maintenance planning and resource allocation\n\n**Implementation:**\n```csharp\npublic class CalibrationScheduleService\n{\n    private static readonly Dictionary<string, int> DefaultIntervals = new()\n    {\n        [\"O2_SENSOR\"] = 180,      // 6 months\n        [\"ECG_MONITOR\"] = 365,    // 12 months\n        [\"FLOW_METER\"] = 90,      // 3 months\n        [\"DEFAULT\"] = 365         // 12 months fallback\n    };\n    \n    public DateTime CalculateNextCalibrationDue(Device device, DateTime lastCalibration)\n    {\n        var interval = DefaultIntervals.GetValueOrDefault(device.ModelId, DefaultIntervals[\"DEFAULT\"]);\n        return lastCalibration.AddDays(interval);\n    }\n}\n```\n\n### D-04: Status Change History with Immutable Audit Trail\n\n**Decision:** Maintain complete status change history in a separate `DeviceStatusHistory` table with immutable records and encrypted change reasons.\n\n**Rationale:**\n- FDA 21 CFR Part 820.180 requires complete device history records\n- Immutable audit trail prevents tampering with compliance records\n- Supports regulatory audits and quality management reviews\n- Enables trend analysis for maintenance optimization\n\n### D-05: Domain Events for Status Change Integration\n\n**Decision:** Publish domain events for all status changes to enable loose coupling with inventory systems and notification services.\n\n**Rationale:**\n- Maintains clean architecture separation between maintenance and inventory domains\n- Enables real-time inventory updates without tight coupling\n- Supports future integration with external systems (ERP, CMMS)\n- Provides foundation for analytics and reporting systems\n\n**Event Structure:**\n```csharp\npublic class DeviceStatusChangedEvent : DomainEvent\n{\n    public Guid DeviceId { get; }\n    public string SerialNumber { get; }\n    public DeviceStatus PreviousStatus { get; }\n    public DeviceStatus NewStatus { get; }\n    public Guid ChangedByUserId { get; }\n    public string ChangeReason { get; }\n    public DateTime ChangeTimestamp { get; }\n    public Guid TenantId { get; }\n}\n```\n\n## Alternatives Considered\n\n### Alternative A-01: Simple Status Field Without State Machine\n**Rejected:** Lacks validation of business rules and could allow invalid transitions that compromise patient safety or regulatory compliance.\n\n### Alternative A-02: Workflow Engine (Windows Workflow Foundation)\n**Rejected:** Adds significant complexity and external dependencies that exceed current platform capabilities and maintenance requirements.\n\n### Alternative A-03: Event Sourcing for Status Changes\n**Rejected:** While providing complete audit trails, event sourcing complexity exceeds regulatory requirements that can be met with traditional audit logging.\n\n### Alternative A-04: Manual Calibration Scheduling\n**Rejected:** Manual processes increase risk of compliance violations and don't scale with fleet size growth.\n\n## Consequences\n\n### Positive Consequences\n- **Regulatory Compliance:** Complete audit trails and validation rules meet FDA and ISO requirements\n- **Patient Safety:** State machine prevents deployment of devices that haven't completed required maintenance\n- **Operational Efficiency:** Automated calibration scheduling optimizes maintenance resource allocation\n- **Integration Flexibility:** Domain events enable loose coupling with external systems\n- **Data Integrity:** Immutable audit trail prevents tampering with compliance records\n- **Scalability:** Efficient status queries support large device fleets per tenant\n\n### Negative Consequences / Trade-offs\n- **Complexity:** State machine logic requires careful testing and validation\n- **Storage Overhead:** Complete audit trail increases database storage requirements\n- **Performance Impact:** Status change validation adds processing overhead\n- **Migration Effort:** Existing devices require status initialization and validation\n\n### Risks and Mitigations\n\n| **Risk** | **Impact** | **Mitigation** |\n|----------|------------|----------------|\n| Invalid state transitions compromise safety | High | Comprehensive unit tests + integration tests + code reviews |\n| Audit trail storage growth | Medium | Automated archiving + data retention policies |\n| Domain event processing failures | Medium | Retry mechanisms + dead letter queues + monitoring |\n| Calibration schedule calculation errors | High | Extensive testing + manual override capabilities |\n\n## Compliance & Security Impact\n\n- **Clean Architecture:** State machine logic in `src/Domain/Maintenance/Services/DeviceStatusService.cs` with clear separation from infrastructure concerns\n- **Multi-Tenancy & Data Isolation:** All status changes scoped to tenant with global query filters on audit tables\n- **HIPAA / Auditability:** \n  - Complete audit trail of all status changes with encrypted change reasons\n  - Immutable history records prevent tampering\n  - User identification for all status changes\n- **GDPR:** \n  - Right to erasure via soft delete patterns on audit records\n  - Data minimization through encrypted change reasons\n- **FDA 21 CFR Part 820:** \n  - Complete device history records (§820.180)\n  - Traceability throughout device lifecycle (§820.70)\n  - Documented procedures for maintenance and calibration\n\n## Validation\n\n- [ ] State machine validation with all possible transition scenarios\n- [ ] Regulatory compliance review with quality assurance team\n- [ ] Performance testing with high-frequency status changes\n- [ ] Integration testing with inventory synchronization\n- [ ] Audit trail completeness verification\n- [ ] Calibration schedule accuracy testing with various device types\n- [ ] Domain event processing reliability testing"
  },
  {
    "number": 4,
    "title": "CQRS Implementation with MediatR for Maintenance Workflows",
    "rendered_markdown": "# ADR-004: CQRS Implementation with MediatR for Maintenance Workflows\n\n## Status\n\n| **Status** | Proposed |\n|------------|----------|\n| **Date** | 2024-12-19 |\n| **Authors** | Enterprise Solution Architecture |\n| **Reviewers** | Application Architecture, Performance Engineering |\n| **Stakeholders** | Healthcare Vendors, Development Teams |\n\n## Context\n\nThe Device Maintenance & Calibration Workflow requires a robust application layer that supports:\n- Complex maintenance workflows with multiple steps and validation requirements\n- High-performance read operations for device fleet management and reporting\n- Audit trail generation for regulatory compliance (FDA 21 CFR Part 820)\n- Multi-tenant isolation with efficient query patterns\n- Integration with existing MediatR CQRS patterns in `src/Application/`\n\nThe existing platform provides MediatR infrastructure with `IRequest<T>`, `IRequestHandler<T>`, and FluentValidation pipeline behaviors, but lacks maintenance-specific command and query implementations.\n\n## Decisions\n\n### D-01: Separate Command and Query Models with Optimized DTOs\n\n**Decision:** Implement distinct command and query models with specialized DTOs optimized for their respective use cases.\n\n**Rationale:**\n- Commands focus on business operations with full validation and audit requirements\n- Queries optimize for read performance with denormalized data and efficient projections\n- Supports different scaling patterns (write vs read operations)\n- Aligns with existing CQRS patterns in `src/Application/Common/`\n\n**Command Structure:**\n```csharp\n// Commands for write operations\npublic class StartMaintenanceWorkflowCommand : IRequest<MaintenanceWorkflowDto>\n{\n    public Guid DeviceId { get; set; }\n    public Guid TechnicianId { get; set; }\n    public string Notes { get; set; }\n    public bool RequiresDeepRepair { get; set; }\n}\n\npublic class CompleteSanitizationCommand : IRequest<SanitizationChecklistDto>\n{\n    public Guid MaintenanceWorkflowId { get; set; }\n    public bool ChemicalCleaningCompleted { get; set; }\n    public string ChemicalUsed { get; set; }\n    public bool HepaFilterReplaced { get; set; }\n    public string FilterPartNumber { get; set; }\n    public IFormFile InspectionPhoto { get; set; }\n}\n```\n\n**Query Structure:**\n```csharp\n// Queries for read operations\npublic class GetDeviceFleetQuery : IRequest<PagedResult<DeviceFleetItemDto>>\n{\n    public int Page { get; set; } = 1;\n    public int PageSize { get; set; } = 50;\n    public DeviceStatus? StatusFilter { get; set; }\n    public string SearchTerm { get; set; }\n    public DateTime? CalibrationDueBefore { get; set; }\n}\n\npublic class GetMaintenanceHistoryQuery : IRequest<PagedResult<MaintenanceHistoryDto>>\n{\n    public Guid DeviceId { get; set; }\n    public DateTime? FromDate { get; set; }\n    public DateTime? ToDate { get; set; }\n    public int Page { get; set; } = 1;\n    public int PageSize { get; set; } = 25;\n}\n```\n\n### D-02: Comprehensive FluentValidation with Business Rule Enforcement\n\n**Decision:** Implement comprehensive validation using FluentValidation with business rule enforcement and regulatory compliance checks.\n\n**Rationale:**\n- FDA 21 CFR Part 820.70(i) requires validation of data integrity\n- Business rules prevent invalid maintenance operations that could compromise patient safety\n- Centralized validation logic ensures consistency across all entry points\n- Leverages existing validation pipeline in `src/Application/Common/Behaviors/`\n\n**Validation Examples:**\n```csharp\npublic class StartMaintenanceWorkflowCommandValidator : AbstractValidator<StartMaintenanceWorkflowCommand>\n{\n    public StartMaintenanceWorkflowCommandValidator(IDeviceRepository deviceRepository)\n    {\n        RuleFor(x => x.DeviceId)\n            .NotEmpty()\n            .MustAsync(async (deviceId, cancellation) => \n            {\n                var device = await deviceRepository.GetByIdAsync(deviceId);\n                return device?.DeviceStatus == DeviceStatus.NeedsMaintenance || \n                       device?.DeviceStatus == DeviceStatus.CalibrationDue;\n            })\n            .WithMessage(\"Device must be in 'Needs Maintenance' or 'Calibration Due' status\");\n            \n        RuleFor(x => x.TechnicianId)\n            .NotEmpty()\n            .WithMessage(\"Technician ID is required for audit trail\");\n            \n        RuleFor(x => x.Notes)\n            .MaximumLength(2000)\n            .WithMessage(\"Notes cannot exceed 2000 characters\");\n    }\n}\n\npublic class RecordCalibrationResultCommandValidator : AbstractValidator<RecordCalibrationResultCommand>\n{\n    public RecordCalibrationResultCommandValidator()\n    {\n        RuleFor(x => x.MeasuredValue)\n            .GreaterThan(0)\n            .WithMessage(\"Measured value must be positive\");\n            \n        RuleFor(x => x.TargetValue)\n            .GreaterThan(0)\n            .WithMessage(\"Target value must be positive\");\n            \n        RuleFor(x => x.ToleranceRange)\n            .GreaterThanOrEqualTo(0)\n            .WithMessage(\"Tolerance range cannot be negative\");\n            \n        RuleFor(x => x)\n            .Must(x => Math.Abs(x.MeasuredValue - x.TargetValue) / x.TargetValue * 100 <= 50)\n            .WithMessage(\"Measured value deviation exceeds 50% - please verify measurement\");\n    }\n}\n```\n\n### D-03: Optimized Query Projections with EF Core Select Statements\n\n**Decision:** Implement optimized query projections using EF Core `Select()` statements to minimize data transfer and improve performance.\n\n**Rationale:**\n- Large device fleets require efficient queries to maintain sub-500ms response times\n- Projections reduce memory usage and network traffic\n- Supports efficient pagination with proper indexing\n- Enables specialized DTOs for different UI components\n\n**Query Implementation:**\n```csharp\npublic class GetDeviceFleetQueryHandler : IRequestHandler<GetDeviceFleetQuery, PagedResult<DeviceFleetItemDto>>\n{\n    public async Task<PagedResult<DeviceFleetItemDto>> Handle(GetDeviceFleetQuery request, CancellationToken cancellationToken)\n    {\n        var query = _context.Devices\n            .Where(d => !d.IsDeleted)\n            .AsQueryable();\n            \n        if (request.StatusFilter.HasValue)\n            query = query.Where(d => d.DeviceStatus == request.StatusFilter.Value);\n            \n        if (!string.IsNullOrEmpty(request.SearchTerm))\n            query = query.Where(d => d.SerialNumber.Contains(request.SearchTerm) || \n                                   d.DeviceName.Contains(request.SearchTerm));\n                                   \n        var totalCount = await query.CountAsync(cancellationToken);\n        \n        var items = await query\n            .OrderBy(d => d.SerialNumber)\n            .Skip((request.Page - 1) * request.PageSize)\n            .Take(request.PageSize)\n            .Select(d => new DeviceFleetItemDto\n            {\n                Id = d.Id,\n                SerialNumber = d.SerialNumber,\n                DeviceName = d.DeviceName,\n                ModelId = d.ModelId,\n                DeviceStatus = d.DeviceStatus,\n                LastMaintenanceDate = d.LastMaintenanceDate,\n                NextCalibrationDue = d.NextCalibrationDue,\n                DaysUntilCalibrationDue = d.NextCalibrationDue.HasValue ? \n                    (int)(d.NextCalibrationDue.Value - DateTime.UtcNow).TotalDays : null\n            })\n            .ToListAsync(cancellationToken);\n            \n        return new PagedResult<DeviceFleetItemDto>(items, totalCount, request.Page, request.PageSize);\n    }\n}\n```\n\n### D-04: Domain Event Publishing for Audit Trail and Integration\n\n**Decision:** Publish domain events from command handlers to maintain audit trails and enable loose coupling with external systems.\n\n**Rationale:**\n- Regulatory compliance requires complete audit trails of all maintenance activities\n- Domain events enable integration with inventory systems without tight coupling\n- Supports future analytics and reporting requirements\n- Leverages existing domain event infrastructure in `src/Domain/Common/`\n\n**Event Publishing Pattern:**\n```csharp\npublic class CompleteMaintenanceWorkflowCommandHandler : IRequestHandler<CompleteMaintenanceWorkflowCommand, MaintenanceWorkflowDto>\n{\n    public async Task<MaintenanceWorkflowDto> Handle(CompleteMaintenanceWorkflowCommand request, CancellationToken cancellationToken)\n    {\n        var workflow = await _workflowRepository.GetByIdAsync(request.WorkflowId);\n        var device = await _deviceRepository.GetByIdAsync(workflow.DeviceId);\n        \n        // Complete workflow and update device status\n        workflow.Complete(request.CompletionNotes);\n        device.UpdateStatus(DeviceStatus.ReadyForDeployment, request.CompletedByUserId, \"Maintenance workflow completed\");\n        \n        // Publish domain events\n        workflow.AddDomainEvent(new MaintenanceWorkflowCompletedEvent\n        {\n            WorkflowId = workflow.Id,\n            DeviceId = device.Id,\n            CompletedAt = DateTime.UtcNow,\n            CompletedByUserId = request.CompletedByUserId,\n            TenantId = workflow.TenantId\n        });\n        \n        device.AddDomainEvent(new DeviceStatusChangedEvent\n        {\n            DeviceId = device.Id,\n            PreviousStatus = DeviceStatus.InMaintenance,\n            NewStatus = DeviceStatus.ReadyForDeployment,\n            ChangedByUserId = request.CompletedByUserId,\n            ChangeReason = \"Maintenance workflow completed\",\n            TenantId = device.TenantId\n        });\n        \n        await _unitOfWork.SaveChangesAsync(cancellationToken);\n        \n        return _mapper.Map<MaintenanceWorkflowDto>(workflow);\n    }\n}\n```\n\n### D-05: Structured Error Handling with RFC 7807 Problem Details\n\n**Decision:** Implement comprehensive error handling using RFC 7807 Problem Details with maintenance-specific error codes and regulatory compliance information.\n\n**Rationale:**\n- Consistent error responses improve API usability and debugging\n- Regulatory compliance requires detailed error logging and traceability\n- Structured errors support automated error handling in client applications\n- Aligns with existing error handling patterns in `src/Api/Common/`\n\n**Error Handling Implementation:**\n```csharp\npublic class MaintenanceErrorCodes\n{\n    public const string DeviceNotFound = \"DEVICE_NOT_FOUND\";\n    public const string InvalidDeviceStatus = \"INVALID_DEVICE_STATUS\";\n    public const string CalibrationOutOfTolerance = \"CALIBRATION_OUT_OF_TOLERANCE\";\n    public const string TechnicianNotAuthorized = \"TECHNICIAN_NOT_AUTHORIZED\";\n    public const string WorkflowAlreadyCompleted = \"WORKFLOW_ALREADY_COMPLETED\";\n}\n\npublic class MaintenanceException : Exception\n{\n    public string ErrorCode { get; }\n    public Dictionary<string, object> Details { get; }\n    \n    public MaintenanceException(string errorCode, string message, Dictionary<string, object> details = null) \n        : base(message)\n    {\n        ErrorCode = errorCode;\n        Details = details ?? new Dictionary<string, object>();\n    }\n}\n```\n\n## Alternatives Considered\n\n### Alternative A-01: Single Model for Commands and Queries\n**Rejected:** Violates CQRS principles and would compromise read performance optimization and write operation validation requirements.\n\n### Alternative A-02: Direct Repository Access from Controllers\n**Rejected:** Bypasses validation, audit trail generation, and business rule enforcement required for regulatory compliance.\n\n### Alternative A-03: Synchronous Event Processing\n**Rejected:** Could impact response times and doesn't provide the resilience needed for critical audit trail generation.\n\n### Alternative A-04: Generic CRUD Operations\n**Rejected:** Maintenance workflows require specialized business logic that generic operations cannot accommodate safely.\n\n## Consequences\n\n### Positive Consequences\n- **Regulatory Compliance:** Comprehensive validation and audit trails meet FDA and ISO requirements\n- **Performance:** Optimized queries support large device fleets with sub-500ms response times\n- **Maintainability:** Clear separation of concerns with CQRS pattern\n- **Scalability:** Separate read/write optimization supports different scaling patterns\n- **Integration:** Domain events enable loose coupling with external systems\n- **Error Handling:** Structured errors improve debugging and client application development\n\n### Negative Consequences / Trade-offs\n- **Complexity:** CQRS pattern requires more code than simple CRUD operations\n- **Learning Curve:** Developers need understanding of MediatR and CQRS patterns\n- **Testing Overhead:** Separate command and query handlers require comprehensive test coverage\n- **Memory Usage:** Validation and event publishing increase memory consumption per request\n\n### Risks and Mitigations\n\n| **Risk** | **Impact** | **Mitigation** |\n|----------|------------|----------------|\n| Validation performance bottleneck | Medium | Async validation + caching + performance monitoring |\n| Domain event processing failures | High | Retry mechanisms + dead letter queues + monitoring |\n| Query performance degradation | Medium | Database indexing + query optimization + caching |\n| Command handler complexity | Medium | Comprehensive unit tests + code reviews + documentation |\n\n## Compliance & Security Impact\n\n- **Clean Architecture:** Application layer in `src/Application/Maintenance/` with clear separation from domain and infrastructure\n- **Multi-Tenancy & Data Isolation:** All commands and queries scoped to current tenant via `ICurrentUserService`\n- **HIPAA / Auditability:** \n  - Complete audit trails via domain events\n  - User identification in all command operations\n  - Structured error logging with correlation IDs\n- **GDPR:** \n  - Data minimization through optimized query projections\n  - Right to erasure support via soft delete patterns\n- **FDA 21 CFR Part 820:** \n  - Data integrity validation (§820.70(i))\n  - Complete traceability of maintenance operations\n  - Documented procedures through command validation\n\n## Validation\n\n- [ ] Performance testing with 1000+ concurrent maintenance operations\n- [ ] Validation rule testing with invalid data scenarios\n- [ ] Domain event processing reliability testing\n- [ ] Query optimization validation with large datasets\n- [ ] Error handling completeness testing\n- [ ] Integration testing with existing MediatR infrastructure\n- [ ] Regulatory compliance validation with audit trail completeness"
  },
  {
    "number": 5,
    "title": "File Upload and Document Management with HIPAA Compliance",
    "rendered_markdown": "# ADR-005: File Upload and Document Management with HIPAA Compliance\n\n## Status\n\n| **Status** | Proposed |\n|------------|----------|\n| **Date** | 2024-12-19 |\n| **Authors** | Enterprise Solution Architecture |\n| **Reviewers** | Security Architecture, Compliance Team |\n| **Stakeholders** | Healthcare Vendors, Regulatory Affairs |\n\n## Context\n\nThe Device Maintenance & Calibration Workflow requires secure document management for:\n- Calibration certificates (PDF, PNG) with digital signature validation\n- Inspection photos from sanitization checklists\n- Technician sign-off reports and quality control documentation\n- Regulatory compliance documents for FDA and ISO audits\n- Multi-tenant isolation with encrypted file storage\n\nThe existing platform provides file upload patterns but lacks maintenance-specific document management with regulatory compliance features. HIPAA requirements mandate encryption at rest, access controls, and complete audit trails for all document operations.\n\n## Decisions\n\n### D-01: Encrypted File Storage with Tenant-Scoped Directory Structure\n\n**Decision:** Implement encrypted file storage using Azure Blob Storage or AWS S3 with tenant-scoped directory structure and server-side encryption.\n\n**Rationale:**\n- HIPAA requires encryption of PHI at rest (§164.312(a)(2)(iv))\n- Tenant-scoped directories provide additional isolation layer\n- Cloud storage provides scalability and built-in redundancy\n- Server-side encryption reduces key management complexity\n- Supports regulatory audit requirements for document retention\n\n**Directory Structure:**\n```\n/maintenance-documents/\n  /{tenant-id}/\n    /calibration-certificates/\n      /{device-id}/\n        /{workflow-id}/\n          /certificate-{timestamp}.pdf\n    /inspection-photos/\n      /{device-id}/\n        /{workflow-id}/\n          /sanitization-{timestamp}.jpg\n    /technician-reports/\n      /{technician-id}/\n        /{workflow-id}/\n          /signoff-{timestamp}.pdf\n```\n\n### D-02: Resumable Upload with Chunked Transfer and Integrity Validation\n\n**Decision:** Implement resumable file uploads using chunked transfer with SHA-256 integrity validation and progress tracking.\n\n**Rationale:**\n- Large calibration certificates (up to 50MB) require reliable upload mechanism\n- Network interruptions are common in healthcare facility environments\n- Integrity validation prevents file corruption during transfer\n- Progress tracking improves user experience for large files\n- Supports concurrent uploads from multiple technicians\n\n**Upload Implementation:**\n```csharp\npublic class ResumableFileUploadService\n{\n    public async Task<UploadSessionDto> InitiateUploadAsync(InitiateUploadRequest request)\n    {\n        var uploadSession = new UploadSession\n        {\n            Id = Guid.NewGuid(),\n            FileName = request.FileName,\n            FileSize = request.FileSize,\n            ContentType = request.ContentType,\n            ChunkSize = CalculateOptimalChunkSize(request.FileSize),\n            ExpectedHash = request.FileHash,\n            TenantId = _currentUser.TenantId,\n            CreatedAt = DateTime.UtcNow\n        };\n        \n        await _uploadRepository.CreateAsync(uploadSession);\n        return _mapper.Map<UploadSessionDto>(uploadSession);\n    }\n    \n    public async Task<ChunkUploadResult> UploadChunkAsync(Guid sessionId, int chunkIndex, Stream chunkData)\n    {\n        var session = await _uploadRepository.GetByIdAsync(sessionId);\n        ValidateChunkUpload(session, chunkIndex, chunkData.Length);\n        \n        var chunkHash = await CalculateChunkHashAsync(chunkData);\n        var chunkPath = GenerateChunkPath(session, chunkIndex);\n        \n        await _storageService.UploadChunkAsync(chunkPath, chunkData);\n        \n        session.RecordChunkUpload(chunkIndex, chunkHash);\n        await _uploadRepository.UpdateAsync(session);\n        \n        return new ChunkUploadResult\n        {\n            ChunkIndex = chunkIndex,\n            IsComplete = session.IsComplete,\n            NextChunkIndex = session.GetNextMissingChunk()\n        };\n    }\n}\n```\n\n### D-03: Digital Signature Validation for Calibration Certificates\n\n**Decision:** Implement digital signature validation for PDF calibration certificates using industry-standard cryptographic libraries.\n\n**Rationale:**\n- FDA 21 CFR Part 11 requires electronic signature validation for regulatory documents\n- Digital signatures provide non-repudiation and integrity verification\n- Supports compliance with ISO 13485 quality management requirements\n- Prevents acceptance of tampered or fraudulent calibration certificates\n- Enables automated validation workflows\n\n**Signature Validation Implementation:**\n```csharp\npublic class DigitalSignatureValidator\n{\n    public async Task<SignatureValidationResult> ValidatePdfSignatureAsync(Stream pdfStream)\n    {\n        using var document = PdfReader.Open(pdfStream, PdfDocumentOpenMode.ReadOnly);\n        var signatures = ExtractDigitalSignatures(document);\n        \n        var results = new List<SignatureInfo>();\n        \n        foreach (var signature in signatures)\n        {\n            var validationResult = await ValidateSignatureAsync(signature);\n            results.Add(new SignatureInfo\n            {\n                SignerName = signature.SignerName,\n                SigningTime = signature.SigningTime,\n                IsValid = validationResult.IsValid,\n                CertificateChainValid = validationResult.CertificateChainValid,\n                DocumentIntegrityValid = validationResult.DocumentIntegrityValid,\n                ValidationErrors = validationResult.Errors\n            });\n        }\n        \n        return new SignatureValidationResult\n        {\n            HasValidSignatures = results.Any(r => r.IsValid),\n            SignatureCount = results.Count,\n            Signatures = results,\n            ValidationTimestamp = DateTime.UtcNow\n        };\n    }\n}\n```\n\n### D-04: Document Lifecycle Management with Expiration Tracking\n\n**Decision:** Implement document lifecycle management with automatic expiration tracking and renewal notifications for calibration certificates.\n\n**Rationale:**\n- Calibration certificates have regulatory expiration dates that must be tracked\n- Expired certificates invalidate device compliance status\n- Automated notifications prevent compliance violations\n- Supports proactive maintenance planning and scheduling\n- Enables regulatory audit trail for document validity periods\n\n**Lifecycle Management:**\n```csharp\npublic class DocumentLifecycleService\n{\n    public async Task ProcessExpiringDocumentsAsync()\n    {\n        var expiringDocuments = await _documentRepository\n            .GetExpiringDocumentsAsync(DateTime.UtcNow.AddDays(30));\n            \n        foreach (var document in expiringDocuments)\n        {\n            await SendExpirationNotificationAsync(document);\n            \n            if (document.ExpirationDate <= DateTime.UtcNow)\n            {\n                await HandleExpiredDocumentAsync(document);\n            }\n        }\n    }\n    \n    private async Task HandleExpiredDocumentAsync(MaintenanceDocument document)\n    {\n        // Update device status if calibration certificate expired\n        if (document.DocumentType == DocumentType.CalibrationCertificate)\n        {\n            var device = await _deviceRepository.GetByMaintenanceWorkflowAsync(document.MaintenanceWorkflowId);\n            if (device.DeviceStatus == DeviceStatus.Available)\n            {\n                device.UpdateStatus(DeviceStatus.CalibrationDue, \n                    SystemUserId, \n                    $\"Calibration certificate expired: {document.FileName}\");\n                    \n                await _deviceRepository.UpdateAsync(device);\n            }\n        }\n    }\n}\n```\n\n### D-05: Comprehensive Access Control with Audit Logging\n\n**Decision:** Implement role-based access control for document operations with comprehensive audit logging of all file access and modifications.\n\n**Rationale:**\n- HIPAA requires access controls and audit trails for all PHI access (§164.312(a)(1))\n- Different roles need different document access levels (technicians, supervisors, auditors)\n- Audit trails support regulatory compliance and security investigations\n- Prevents unauthorized access to sensitive calibration and maintenance documents\n\n**Access Control Implementation:**\n```csharp\npublic class DocumentAccessService\n{\n    public async Task<DocumentAccessResult> ValidateAccessAsync(Guid documentId, DocumentOperation operation)\n    {\n        var document = await _documentRepository.GetByIdAsync(documentId);\n        var user = await _currentUserService.GetCurrentUserAsync();\n        \n        // Tenant isolation check\n        if (document.TenantId != user.TenantId)\n        {\n            await LogAccessViolationAsync(user.Id, documentId, \"Cross-tenant access attempt\");\n            return DocumentAccessResult.Denied(\"Document not found\");\n        }\n        \n        // Role-based access control\n        var hasAccess = operation switch\n        {\n            DocumentOperation.Read => user.HasRole(\"Technician\") || user.HasRole(\"Supervisor\") || user.HasRole(\"Auditor\"),\n            DocumentOperation.Upload => user.HasRole(\"Technician\") || user.HasRole(\"Supervisor\"),\n            DocumentOperation.Delete => user.HasRole(\"Supervisor\"),\n            _ => false\n        };\n        \n        // Log access attempt\n        await LogDocumentAccessAsync(new DocumentAccessLog\n        {\n            UserId = user.Id,\n            DocumentId = documentId,\n            Operation = operation,\n            AccessGranted = hasAccess,\n            AccessTimestamp = DateTime.UtcNow,\n            UserAgent = _httpContext.Request.Headers[\"User-Agent\"],\n            IpAddress = _httpContext.Connection.RemoteIpAddress?.ToString()\n        });\n        \n        return hasAccess ? DocumentAccessResult.Granted() : DocumentAccessResult.Denied(\"Insufficient permissions\");\n    }\n}\n```\n\n## Alternatives Considered\n\n### Alternative A-01: Local File System Storage\n**Rejected:** Lacks scalability, redundancy, and built-in encryption capabilities required for healthcare data. Complicates backup and disaster recovery procedures.\n\n### Alternative A-02: Database BLOB Storage\n**Rejected:** Large files would impact database performance and backup procedures. Doesn't provide the scalability needed for document management at enterprise scale.\n\n### Alternative A-03: Simple HTTP Upload Without Resumability\n**Rejected:** Large calibration certificates and unreliable healthcare facility networks require resumable upload capability for acceptable user experience.\n\n### Alternative A-04: Client-Side Encryption Only\n**Rejected:** While providing additional security, client-side encryption complicates key management and doesn't eliminate the need for server-side encryption for compliance.\n\n## Consequences\n\n### Positive Consequences\n- **Regulatory Compliance:** Encrypted storage and audit trails meet HIPAA, FDA, and ISO requirements\n- **Reliability:** Resumable uploads handle network interruptions common in healthcare facilities\n- **Security:** Multi-layered access controls prevent unauthorized document access\n- **Integrity:** Digital signature validation ensures document authenticity\n- **Scalability:** Cloud storage supports growth in document volume and concurrent users\n- **Auditability:** Complete access logs support regulatory audits and security investigations\n\n### Negative Consequences / Trade-offs\n- **Complexity:** Resumable uploads and signature validation add significant implementation complexity\n- **Storage Costs:** Encrypted cloud storage and redundancy increase operational costs\n- **Performance Impact:** Signature validation and encryption add processing overhead\n- **Key Management:** Encryption requires robust key management and rotation procedures\n\n### Risks and Mitigations\n\n| **Risk** | **Impact** | **Mitigation** |\n|----------|------------|----------------|\n| Encryption key compromise | High | Key rotation procedures + HSM integration + access monitoring |\n| Upload failures during large transfers | Medium | Resumable upload + retry mechanisms + progress monitoring |\n| Digital signature validation errors | Medium | Fallback to manual validation + comprehensive error logging |\n| Storage service outages | Medium | Multi-region redundancy + local caching + graceful degradation |\n\n## Compliance & Security Impact\n\n- **Clean Architecture:** Document management in `src/Application/Documents/` with clear separation from storage infrastructure\n- **Multi-Tenancy & Data Isolation:** Tenant-scoped directory structure + access validation prevent cross-tenant document access\n- **HIPAA / Auditability:** \n  - Encryption at rest and in transit (§164.312(a)(2)(iv), §164.312(e)(1))\n  - Access controls and audit trails (§164.312(a)(1), §164.312(b))\n  - Unique user identification for all document operations (§164.312(d))\n- **GDPR:** \n  - Right to erasure via document deletion capabilities\n  - Data minimization through access controls\n  - Processing transparency via audit logs\n- **FDA 21 CFR Part 11:** \n  - Electronic signature validation for regulatory documents\n  - Document integrity and non-repudiation\n  - Complete audit trails for electronic records\n\n## Validation\n\n- [ ] Upload performance testing with 50MB files over various network conditions\n- [ ] Digital signature validation testing with various certificate authorities\n- [ ] Access control testing with cross-tenant access attempts\n- [ ] Encryption validation with security team review\n- [ ] Document lifecycle testing with expiration scenarios\n- [ ] Audit trail completeness verification\n- [ ] Disaster recovery testing with document restoration\n- [ ] Compliance validation with regulatory audit simulation"
  },
  {
    "number": 6,
    "title": "Next.js Frontend Architecture with Server-First RSC Pattern",
    "rendered_markdown": "# ADR-006: Next.js Frontend Architecture with Server-First RSC Pattern\n\n## Status\n\n| **Status** | Proposed |\n|------------|----------|\n| **Date** | 2024-12-19 |\n| **Authors** | Enterprise Solution Architecture |\n| **Reviewers** | Frontend Architecture, UX Engineering |\n| **Stakeholders** | Healthcare Vendors, Development Teams |\n\n## Context\n\nThe Device Maintenance & Calibration Workflow requires a modern frontend architecture that supports:\n- Complex maintenance workflows with real-time status updates\n- Large device fleet management with efficient data loading and filtering\n- File upload interfaces for calibration certificates and inspection photos\n- Multi-tenant isolation with secure authentication integration\n- Regulatory compliance with audit trail generation for user interactions\n- Mobile-responsive design for technicians using tablets in healthcare facilities\n\nThe existing platform uses Next.js 15 with App Router, React Server Components, and TypeScript, but lacks maintenance-specific UI components and workflow management patterns.\n\n## Decisions\n\n### D-01: Server-First RSC Architecture with Strategic Client Components\n\n**Decision:** Implement a server-first architecture using React Server Components for data fetching and page rendering, with selective client components for interactive features.\n\n**Rationale:**\n- RSC provides better performance for data-heavy device fleet management\n- Server-side rendering improves initial page load times critical for healthcare workflows\n- Reduces client-side JavaScript bundle size for better mobile performance\n- Enables secure data fetching without exposing API endpoints to client\n- Supports SEO and accessibility requirements for healthcare compliance\n\n**Architecture Pattern:**\n```typescript\n// Server Component for data fetching\nexport default async function DeviceFleetPage({\n  searchParams\n}: {\n  searchParams: { status?: string; search?: string; page?: string }\n}) {\n  const devices = await getDeviceFleet({\n    status: searchParams.status as DeviceStatus,\n    search: searchParams.search,\n    page: parseInt(searchParams.page || '1')\n  });\n  \n  return (\n    <div className=\"space-y-6\">\n      <DeviceFleetHeader totalDevices={devices.totalCount} />\n      <DeviceFleetFilters /> {/* Client Component for interactivity */}\n      <DeviceFleetGrid devices={devices.items} /> {/* Server Component */}\n      <DeviceFleetPagination \n        currentPage={devices.currentPage}\n        totalPages={devices.totalPages}\n      /> {/* Client Component for navigation */}\n    </div>\n  );\n}\n\n// Client Component for interactive filtering\n'use client';\nexport function DeviceFleetFilters() {\n  const router = useRouter();\n  const searchParams = useSearchParams();\n  \n  const handleStatusFilter = (status: DeviceStatus | null) => {\n    const params = new URLSearchParams(searchParams);\n    if (status) {\n      params.set('status', status);\n    } else {\n      params.delete('status');\n    }\n    params.delete('page'); // Reset to first page\n    router.push(`/maintenance/devices?${params.toString()}`);\n  };\n  \n  return (\n    <div className=\"flex gap-4\">\n      <StatusFilterDropdown onStatusChange={handleStatusFilter} />\n      <SearchInput placeholder=\"Search by serial number or device name\" />\n    </div>\n  );\n}\n```\n\n### D-02: URL-Driven State Management with Search Parameters\n\n**Decision:** Use URL search parameters as the primary state management mechanism for filters, pagination, and workflow navigation.\n\n**Rationale:**\n- Enables bookmarkable URLs for specific device views and maintenance workflows\n- Supports browser back/forward navigation within maintenance processes\n- Simplifies state synchronization between server and client components\n- Provides audit trail of user navigation for regulatory compliance\n- Eliminates complex client-side state management for most use cases\n\n**State Management Pattern:**\n```typescript\n// URL structure for maintenance workflows\n/maintenance/devices                           // Device fleet overview\n/maintenance/devices?status=NeedsMaintenance   // Filtered device list\n/maintenance/workflow/[workflowId]             // Maintenance workflow detail\n/maintenance/workflow/[workflowId]/sanitization // Sanitization checklist step\n/maintenance/workflow/[workflowId]/calibration  // Calibration testing step\n/maintenance/workflow/[workflowId]/telemetry    // Telemetry testing step\n\n// Search params for complex filtering\ninterface DeviceFleetSearchParams {\n  status?: DeviceStatus;\n  search?: string;\n  page?: number;\n  sortBy?: 'serialNumber' | 'deviceName' | 'lastMaintenance' | 'calibrationDue';\n  sortOrder?: 'asc' | 'desc';\n  calibrationDueBefore?: string; // ISO date string\n}\n\n// Type-safe search params parsing\nexport function parseDeviceFleetParams(searchParams: URLSearchParams): DeviceFleetSearchParams {\n  return {\n    status: searchParams.get('status') as DeviceStatus || undefined,\n    search: searchParams.get('search') || undefined,\n    page: parseInt(searchParams.get('page') || '1'),\n    sortBy: (searchParams.get('sortBy') as any) || 'serialNumber',\n    sortOrder: (searchParams.get('sortOrder') as any) || 'asc',\n    calibrationDueBefore: searchParams.get('calibrationDueBefore') || undefined\n  };\n}\n```\n\n### D-03: BFF Proxy Pattern with Type-Safe API Integration\n\n**Decision:** Implement Backend-for-Frontend (BFF) proxy pattern using Next.js API routes with comprehensive TypeScript integration and Zod validation.\n\n**Rationale:**\n- Provides secure API proxy without exposing backend endpoints to client\n- Enables request/response transformation optimized for frontend needs\n- Supports authentication token management and refresh logic\n- Allows caching strategies specific to frontend requirements\n- Maintains type safety across the entire frontend-backend boundary\n\n**BFF Implementation:**\n```typescript\n// API route for device fleet data\n// app/api/maintenance/devices/route.ts\nimport { NextRequest } from 'next/server';\nimport { z } from 'zod';\nimport { getServerSession } from 'next-auth';\n\nconst DeviceFleetQuerySchema = z.object({\n  status: z.nativeEnum(DeviceStatus).optional(),\n  search: z.string().optional(),\n  page: z.coerce.number().min(1).default(1),\n  pageSize: z.coerce.number().min(1).max(100).default(50)\n});\n\nexport async function GET(request: NextRequest) {\n  const session = await getServerSession();\n  if (!session?.accessToken) {\n    return Response.json({ error: 'Unauthorized' }, { status: 401 });\n  }\n  \n  const { searchParams } = new URL(request.url);\n  const query = DeviceFleetQuerySchema.parse(Object.fromEntries(searchParams));\n  \n  try {\n    const response = await fetch(`${process.env.API_BASE_URL}/api/maintenance/devices`, {\n      headers: {\n        'Authorization': `Bearer ${session.accessToken}`,\n        'Content-Type': 'application/json'\n      },\n      body: JSON.stringify(query)\n    });\n    \n    if (!response.ok) {\n      throw new Error(`API error: ${response.status}`);\n    }\n    \n    const data = await response.json();\n    return Response.json(data);\n  } catch (error) {\n    console.error('Device fleet API error:', error);\n    return Response.json(\n      { error: 'Faile